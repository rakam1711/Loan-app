let express,APP_PORT,routes;_419‍.w("express",[["default",["express"],function(v){express=v}]]);_419‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}]]);_419‍.w("./routes/index",[["default",["routes"],function(v){routes=v}]]);

//import errorHandler from "./middlewares/errorHandler";

const cors = require("cors");
const db = require("./model/index");
// const https = require("https");
// const fs = require("fs");
// const  fileUpload=require('express-fileupload')

const app = express();

db.sequelize.sync();
app.use(express.urlencoded({ extended: true, limit: "100kb" }));
app.use(express.json());
app.use(cors());
// app.use(morgan("dev"));

app.use("/api", routes);
//app.use(errorHandler);
let port = process.env.APP_PORT || 1001;

app.listen(port, () => console.log(`[ ✓ ] Loan No1 Running on port : ${port}`));
